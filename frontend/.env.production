# Production: same-origin API via Nginx reverse proxy (recommended).
# Frontend calls /api, /uploads, /socket.io on https://cselenaguerra.site
# Nginx must proxy those paths to the Node backend (port 5001).
VITE_API_URL=
